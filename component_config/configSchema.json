{
  "type": "object",
  "title": "RevenueCat Extractor Configuration",
  "required": [
    "#api_key"
  ],
  "properties": {
    "#api_key": {
      "type": "string",
      "format": "password",
      "title": "RevenueCat API Key",
      "description": "V2 secret key (sk_…) for the RevenueCat REST API v2. Mint one in the RevenueCat dashboard under Settings → API keys → Add new key (type: V2 Secret). Grant at minimum read scopes for all project resources.",
      "propertyOrder": 1
    },
    "test_connection": {
      "type": "button",
      "format": "test-connection",
      "propertyOrder": 2,
      "options": {
        "async": {
          "label": "Test Connection",
          "action": "testConnection"
        }
      }
    },
    "project_id": {
      "type": "string",
      "title": "Project",
      "description": "Optional — restrict extraction to one RevenueCat project. Leave empty to extract all projects the API key can access.",
      "enum": [],
      "format": "select",
      "options": {
        "async": {
          "label": "Load projects",
          "action": "listProjects"
        }
      },
      "propertyOrder": 3
    },
    "entities": {
      "type": "array",
      "title": "Entity groups",
      "description": "Which entity groups to extract. 'config' = projects/apps/products/entitlements/offerings/packages (lightweight). 'customers' = customers + active_entitlements/subscriptions/purchases/invoices (heavier fan-out, one API call per customer per entity type).",
      "items": {
        "type": "string",
        "enum": [
          "config",
          "customers"
        ]
      },
      "format": "checkbox",
      "default": [
        "config",
        "customers"
      ],
      "uniqueItems": true,
      "propertyOrder": 4
    }
  }
}
